<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!--
            AdMob App ID, injected from ADMOB_APP_ID in secrets.properties (see the module's
            build.gradle.kts). Falls back to Google's public SAMPLE app ID when unset.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />
    </application>

</manifest>
