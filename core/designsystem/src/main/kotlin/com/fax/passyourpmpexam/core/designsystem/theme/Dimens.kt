package com.fax.passyourpmpexam.core.designsystem.theme

import androidx.compose.ui.unit.dp

/**
 * Spacing tokens from the SSOT: strict 4px grid, 24px base padding / horizontal safe
 * margins, 12px item gaps, 48px minimum touch targets.
 */
object PmpSpacing {
    val gridUnit = 4.dp
    val itemGap = 12.dp
    val safeMargin = 16.dp
    val basePadding = 24.dp
    val touchTargetMin = 48.dp
}
