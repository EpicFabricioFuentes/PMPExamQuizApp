<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".PmpApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/studying_cat_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/studying_cat_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.PassYourPMPExam">

        <!-- Reported crashes are disabled in debug builds via the manifestPlaceholder. Inert until
             app/google-services.json is present and the Firebase plugins are applied. -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${crashlyticsCollectionEnabled}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PassYourPMPExam.Starting"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>